const {test,expect} = require('@playwright/test')

test("Working with multiple tabs", async({browser}) => {

    // Pending multiple tabs in Playwright

    // Pending page , browser , context

    // wait For LoadState 
    
})